{% extends 'base.html.twig' %}

{% block title %}Modifier Contrat{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col s12 m12 l12">
            <h3>Modifier un contrat</h3>
        </div>
    </div>

    {{ include('contract/_form.html.twig') }}
    <a href="{{ path('app_contract_index') }}">Retour à la liste</a>

    {{ include('contract/_delete_form.html.twig') }}
</div>
{% endblock %}
