{% extends 'base.html.twig' %}

{% block title %}Modifier Babysitter{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col s12 m12 l12">
            <h3>Modifier un Babysitter</h3>
    {% if babysitter.picture is not null %}
        <img width="200" src="{{ asset('uploads/' ~ babysitter.picture) }}">
    {% endif %}
        </div>
    </div>

    {{ include('babysitter/_form.html.twig')    }}
    <a href="{{ path('app_admin') }}">Retour à la liste</a>
    
    {{ include('babysitter/_delete_form.html.twig') }}
</div>
{% endblock %}
