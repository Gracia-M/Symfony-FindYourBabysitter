{% extends 'base.html.twig' %}

{% block title %}Home {% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        {% for babysitter in babysitters %}
            <div class="col s12 m4 l3">
                <div class="babysitter-card">
                    <img src="{{ asset('uploads/' ~ babysitter.picture) }}" class="responsive-img">
                    {# <h2><a href="{{ path('app_babysitter_show', {'url': article.url}) }}">{{ article.title }}</a></h2>
                    <span class="date">{{ article.publicationDate|date('d/m/Y') }}</span> #}
                </div>
            </div>
        {% endfor %}
    </div>
    
</div>

{% endblock %}
