{% extends 'base.html.twig' %}

{% block title %}Home {% endblock %}

{% block body %}
{# <div class="container">
    <div class="row">
        {% for babysitter in babysitters %}
            <div class="col s12 m4 l3">
                <div class="babysitter-card">
                    <img src="{{ asset('uploads/' ~ babysitter.picture) }}" class="responsive-img">
                    
                    
                </div>
            </div>
        {% endfor %}
    </div>
    
</div> #}
<div class="container">
    <h3>Babysitter</h3>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Picture</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Gender</th>
                <th>Location</th>
                <th>Description</th>
                <th>IsAvailable</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for babysitter in babysitters %}
            <tr>
                <td>{{ babysitter.id }}</td>
                <td>{{ babysitter.picture }}</td>
                <td>{{ babysitter.firstname }}</td>
                <td>{{ babysitter.lastname }}</td>
                <td>{{ babysitter.gender }}</td>
                <td>{{ babysitter.location }}</td>
                <td>{{ babysitter.description }}</td>
                <td>{{ babysitter.isAvailable ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_babysitter_show', {'id': babysitter.id}) }}">afficher</a>
                    <a href="{{ path('app_babysitter_edit', {'id': babysitter.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">pas encore des babysitters</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_babysitter_new') }}">Ajouter un nouveau</a>
</div>

{% endblock %}
